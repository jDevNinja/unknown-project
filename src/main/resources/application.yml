spring:
  jpa:
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: never
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5442/postgres
    driver-class-name: org.postgresql.Driver
    username: user
    password: pass
